This might not be the latest version, even though i try to keep it up to date! Check `backend/templates/`, it contains the frontend too.

If nothing is there, make sure you cloned the repo recursively.